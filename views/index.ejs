<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mr Dom’s - Marketplace</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body { background-color: #0b0f1a; color: white; font-family: 'Inter', sans-serif; overflow-x: hidden; }
        .glass-card { background: rgba(255, 255, 255, 0.03); backdrop-filter: blur(15px); border: 1px solid rgba(255, 255, 255, 0.08); }
        .nav-active { color: #6366f1; transform: translateY(-3px); }
        .hidden-section { display: none; }
        .gradient-text { background: linear-gradient(135deg, #6366f1, #a855f7); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
        .no-scrollbar::-webkit-scrollbar { display: none; }
    </style>
</head>
<body class="pb-24">

    <nav class="flex justify-between items-center px-6 py-6 sticky top-0 bg-[#0b0f1a]/90 backdrop-blur-xl z-40">
        <h1 class="text-xl font-black tracking-tighter italic">MR DOM’S</h1>
        <div class="flex items-center gap-3">
            <button onclick="showSection('profile')" class="bg-indigo-600/10 text-indigo-400 px-4 py-1.5 rounded-full text-xs font-bold border border-indigo-500/20">
                <i class="fas fa-user-circle mr-1"></i> Connexion
            </button>
        </div>
    </nav>

    <div id="homeSection" class="animate-in fade-in duration-500">
        <header class="px-6 py-6">
